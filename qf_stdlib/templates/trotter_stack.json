{
  "schema": "qf-template-v1",
  "template_id": "trotter_stack",
  "description": "Repeat a canonical sealed TFIM Trotter step and keep approximation scope explicit.",
  "parameters": {
    "repetitions": {
      "type": "int",
      "default": 1
    }
  },
  "requires": [
    {
      "name": "step",
      "canon_key": "trotter/tfim3/first_order",
      "role": "exact sealed first-order step"
    }
  ],
  "assembly": [
    {
      "op": "repeat",
      "name": "step",
      "count_param": "repetitions"
    }
  ],
  "certificate_rule": {
    "type": "exact_step_plus_approximation_metadata",
    "claim": "Each step is registry-attested exactly; approximation to continuous dynamics is metadata, not a seal."
  },
  "limits": [
    "Trotter convergence and backend dynamics are observations unless separately sealed.",
    "Approximation is not exact verification."
  ]
}

