# rm15_tt - RM [[15,1,3]] transversal T-pattern: T^15 (전 큐빗 T). 15q>EXACT_BOUND -> Tier-1 STRUCTURAL only.
# TrackHE5 P4 (#QEC #C3, report5 5/8): ★거리-3 transversal-T 코드의 비-Clifford 층 — 코드공간 사영 ==
#   논리 T† (RM(1,4) 구조: |0̄⟩ 성분무게 ≡0·|1̄⟩ ≡7 mod 8 — 정수 witness, code832 (0,0,1) 의 [[15,1,3]] 판).
#   15-to-1 magic 증류 정준 기판의 게이트 층. 신규 module 0 (t_gate ×15).
# ★제6 경로 자동 소비 예정: 1q 대각 15개 = matchgate_verify 커버(n=15 는 cap 밖 — 정직 기록)·
#   제5 경로는 T-count 15 > 분기 cap — witness 는 rm15_observe 정수 산술이 담당(dense 불요).
# 정직 경계: STRUCTURAL(dense 불가). 논리 T† 작용·거리 = rm15_observe 정수 witness(INV-Q3).
#   증류 성공률·후선택 = 범위 밖.
```json id=app_meta
{"id": "rm15_tt", "n_sys": 15, "n_anc": 0}
```
```json id=plan
{"tier": "structural", "steps": [
  {"spec": "../modules/t_gate.pg", "targets": [0]},
  {"spec": "../modules/t_gate.pg", "targets": [1]},
  {"spec": "../modules/t_gate.pg", "targets": [2]},
  {"spec": "../modules/t_gate.pg", "targets": [3]},
  {"spec": "../modules/t_gate.pg", "targets": [4]},
  {"spec": "../modules/t_gate.pg", "targets": [5]},
  {"spec": "../modules/t_gate.pg", "targets": [6]},
  {"spec": "../modules/t_gate.pg", "targets": [7]},
  {"spec": "../modules/t_gate.pg", "targets": [8]},
  {"spec": "../modules/t_gate.pg", "targets": [9]},
  {"spec": "../modules/t_gate.pg", "targets": [10]},
  {"spec": "../modules/t_gate.pg", "targets": [11]},
  {"spec": "../modules/t_gate.pg", "targets": [12]},
  {"spec": "../modules/t_gate.pg", "targets": [13]},
  {"spec": "../modules/t_gate.pg", "targets": [14]}]}
```
