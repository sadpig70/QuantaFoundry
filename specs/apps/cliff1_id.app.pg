# cliff1_id — 1q Clifford 군 C1 원소 항등원 I (plan=h·h, H²=I exact). V8 Unitary2Design: C1 전체 24원소 봉인 → 정확 unitary 2-design(F2=2)·3-design(F3=5)은 twodesign_observe 관측(seal 아님).
# 켤레작용(mod phase 원소 식별자): X→+X, Z→+Z. plan=기봉인 h_gate/s_gate word, 신규 module 0.
```json id=app_meta
{"id": "cliff1_id", "n_sys": 1, "n_anc": 0}
```
```python id=app_golden
import numpy as np
H = np.array([[1, 1], [1, -1]], dtype=complex) / np.sqrt(2)
S = np.diag([1, 1j]).astype(complex)
golden = np.eye(2, dtype=complex)
```
```json id=plan
{"steps": [{"spec": "../modules/h_gate.pg", "targets": [0]}, {"spec": "../modules/h_gate.pg", "targets": [0]}]}
```
