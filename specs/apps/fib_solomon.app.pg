# fib_solomon — braid word σ₁⁴ (trace 폐포 = Solomon 링크 T(2,4) ⊔ unknot). Tier-0 2×2. TrackHE4 P1 (#TOPO).
# 봉인 fib_braid_s1 의 sub-app 4회 복리 — T(2,n) family 세 번째 표본 (skein 재귀 2단 대조점).
# golden = R⁴ 정의 직접 (닫힌형, 탐색 0). circuit(z5²⁸) ≅ golden up-to-phase(C4).
# Jones 관측: V(T(2,4))@t=e^{−2πi/5} 가중 trace ↔ skein 재귀 일치 = fib_jones_observe(INV-Q3, seal 아님).
# 정직 경계: 봉인=word 유니터리뿐. Jones 값·매듭 동치판정=관측/범위 밖.
```json id=app_meta
{"id": "fib_solomon", "n_sys": 1, "n_anc": 0}
```
```python id=app_golden
import numpy as np
R = np.diag([np.exp(-4j * np.pi / 5), np.exp(3j * np.pi / 5)]).astype(complex)
golden = np.linalg.matrix_power(R, 4)
```
```json id=plan
{"steps": [
  {"app": "fib_braid_s1.app.pg", "targets": [0]},
  {"app": "fib_braid_s1.app.pg", "targets": [0]},
  {"app": "fib_braid_s1.app.pg", "targets": [0]},
  {"app": "fib_braid_s1.app.pg", "targets": [0]}]}
```
