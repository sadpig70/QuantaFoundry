# ry_ak41_dag — ry_ak41 의 dagger (AKLT₄ controlled-Ry 분해 역회전 성분). controlled-Ry 분해(CX 켤레)용 반각. Ry(α)=YPowGate(exponent=α/π) up-to-phase(C4). analytic golden.
```python id=bloq
import numpy as np
from qualtran.bloqs.basic_gates import YPowGate
bloq = YPowGate(exponent=float(-np.arccos(np.sqrt(28.0/41.0)) / np.pi))
```
```python id=golden
import numpy as np
a = -np.arccos(np.sqrt(28.0/41.0))
golden = np.array([[np.cos(a/2), -np.sin(a/2)], [np.sin(a/2), np.cos(a/2)]], dtype=complex)
```
```json id=meta
{"id": "ry_ak41_dag", "n_sys": 1, "n_anc": 0}
```
