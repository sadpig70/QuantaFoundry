# ry_negpi4 — Ry(-π/4) 반각. Ry(α)=YPowGate(exponent=α/π) up-to-phase(C4). analytic golden.
```python id=bloq
import numpy as np
from qualtran.bloqs.basic_gates import YPowGate
bloq = YPowGate(exponent=-0.25)
```
```python id=golden
import numpy as np
a = -np.pi/4
golden = np.array([[np.cos(a/2), -np.sin(a/2)], [np.sin(a/2), np.cos(a/2)]], dtype=complex)
```
```json id=meta
{"id": "ry_negpi4", "n_sys": 1, "n_anc": 0}
```
