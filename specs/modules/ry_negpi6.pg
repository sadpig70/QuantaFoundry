# ry_negpi6 — Ry(−π/6) (TrackGate6 G3 π/6·π/3 family; 정욱님 승인 2026-07-05, .pgf/approvals/G3-ry_pi6.md).
# CRY(π/3) 반각 분해의 역회전 짝(명명 규약: π-분수 family 는 neg-접두, _dag 는 ry_k*/ak*/cg 전용).
# Ry(α)=YPowGate(exponent=α/π) up-to-phase(C4). analytic golden.
```python id=bloq
import numpy as np
from qualtran.bloqs.basic_gates import YPowGate
bloq = YPowGate(exponent=-1.0 / 6.0)
```
```python id=golden
import numpy as np
a = -np.pi / 6.0
golden = np.array([[np.cos(a/2), -np.sin(a/2)], [np.sin(a/2), np.cos(a/2)]], dtype=complex)
```
```json id=meta
{"id": "ry_negpi6", "n_sys": 1, "n_anc": 0}
```
